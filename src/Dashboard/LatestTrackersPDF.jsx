import React from "react";

import {Document, Page} from "@react-pdf/renderer";
 const LatestTrackersPDF = () => (
  <Document>
    <Page wrap>// My document data</Page>
  </Document>
);

// export default LatestTrackersPDF;
